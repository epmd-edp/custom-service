apiVersion: v1
kind: Service
metadata:
  name: {{ include "custom-framework-service.fullname" . }}
  labels:
    {{- include "custom-framework-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "custom-framework-service.selectorLabels" . | nindent 4 }}
