apiVersion: v1
kind: Service
metadata:
  name: {{ include "gin-go.fullname" . }}
  labels:
    {{- include "gin-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gin-go.selectorLabels" . | nindent 4 }}
